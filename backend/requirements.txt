# Litmus — backend runtime dependencies
# Pinned to the versions this system is developed and tested against.

# --- API layer -------------------------------------------------------------
python-dotenv==1.0.1         # loads .env in local development only
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# --- Face pipeline ---------------------------------------------------------
torch==2.2.1
torchvision==0.17.1
pillow==11.0.0
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.9.0          # calibration bundle in face_detector.joblib
joblib==1.4.2
opencv-python==4.8.1.78

# Face detection. facenet-pytorch runs MTCNN on the torch that is already
# loaded. The original mtcnn package needs TensorFlow + Keras, measured at
# 338 MB resident — more than torch and every Litmus model combined, purely to
# locate five landmarks. That single dependency was what pushed the service
# past a 512 MB instance limit on its first request.
facenet-pytorch==2.6.0

# --- Voice pipeline --------------------------------------------------------
transformers==4.53.3
librosa==0.11.0

# --- Speech-presence detection (server-side audio decode) ------------------
av==15.1.0

# --- Records and outbound ---------------------------------------------------
reportlab==4.2.2             # PDF verification records
requests==2.31.0             # DoT FRI lookups, SMTP2GO HTTP API
